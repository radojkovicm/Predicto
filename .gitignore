# Secrets and local config
.env
*.env

# Database files and dumps (may contain real user data — never commit)
*.db
*.sqlite
*.sqlite3
*.dump
*.sql
/db_dump/*
!/db_dump/.gitkeep
# ...except the intentionally-committed, sample-data-only seed for the
# Vercel demo (no real users/predictions — see app/db.py).
!/app/demo_seed.db

# Internal notes with infra details (VPS IP, SSH user, private domain)
setup_deployment.md

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Docker
.dockerignore

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Vercel
.vercel

# Local AI-assistant config
.claude/
